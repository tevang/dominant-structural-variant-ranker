"""Streamlit views for the run inspection GUI.

These modules import ``streamlit`` and the optional ``gui`` extra at import
time; they must only be imported when the extra is installed (the CLI guards
this).
"""
