name: dominant-structural-variant-ranker
channels:
  - conda-forge
dependencies:
  - python>=3.11
  - rdkit
  - numpy
  - pandas
  - pyyaml
  - typer
  - rich
  - pydantic
  - pytest
  - pytest-cov
  - ruff
  - mypy
  - pip
  # Optional Python packages can be installed by the bootstrap scripts:
  # - Auto3D
  # - molscrub
  # Optional external binaries such as xtb, crest, censo, and psi4 should be
  # installed from conda-forge or upstream instructions when needed.

