# File Formats

Input support starts with SMILES and SDF. SMILES files may contain either a
single SMILES column or two columns in `SMILES molname` format. SDF files may be
single-molecule or multi-molecule.

